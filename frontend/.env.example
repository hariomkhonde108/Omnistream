# The three backend services this frontend talks to. Defaults below match
# the dropvault-v2 backend's own docker-compose/config defaults exactly —
# only change these if you've changed the backend's ports.

VITE_API_URL=http://localhost:8080
VITE_INGESTION_URL=http://localhost:8081
VITE_WS_URL=ws://localhost:8080
